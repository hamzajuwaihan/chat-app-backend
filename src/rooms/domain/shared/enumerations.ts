export enum RoomVisibility {
  PUBLIC = 'public',
  PRIVATE = 'private',
  ON_REQUEST = 'on-request',
}

export enum OwnerType {
  SYSTEM = 'system',
  USER = 'user',
}

export enum RoomRole {
  MEMBER = 'member',
  MODERATOR = 'moderator',
  CO_HOST = 'co-host',
  OWNER = 'owner',
}
