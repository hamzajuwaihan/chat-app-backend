export enum UserStatusDto {
  AVAILABLE = 'available',
  BUSY = 'busy',
  AWAY = 'away',
}

export enum PrivacySettingTypeDto {
  ALL = 'all',
  FRIENDS_ONLY = 'friends-only',
  SPECIFIC_USERS = 'specific-users',
  NO_ONE = 'no-one',
}
